build:
    docker:
        web: api/Dockerfile
        web: front/Dockerfile
        web: workers/Dockerfile
    config: 
        test: test
run: 
    web: npm start
    web: npm start
    web: npm start
