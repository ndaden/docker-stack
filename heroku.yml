setup:
    addons:
        plan: cloudamqp
        as: rabbit
    config: 
        FRONTAPPURI: https://mydockerstack1.herokuapp.com
        MONGOURI: test
        MONGOUSER: test
        MONGOPWD: test
        MONGODBNAME: test

build:
    docker:
        web: api/Dockerfile
        worker: workers/Dockerfile
run: 
    web: npm start
    worker: npm start
